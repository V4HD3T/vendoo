DB_HOST=localhost
DB_PORT=5432
DB_NAME=vendoo
DB_USER=postgres
DB_PASSWORD=your_password_here

PORT=3000
NODE_ENV=development

ALLOWED_ORIGINS=http://localhost:5173

JWT_SECRET=write_a_random_string_of_at_least_64_characters_here
JWT_EXPIRES_IN=7d

# Orders & payments — leave true for local dev (no real payment account needed)
PAYMENT_MOCK_MODE=true
